
<%#= cl_image_tag("image.png", width: 400, height: 300, crop: :fill) %>

<center><h1>All items</h1></center>
<div class="card-container2">
  <div class="row">
    <% @items.each do |item| %>
  <div class="col-xs-12 col-sm-4">

  <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)),url(<%= cl_image_path item.photo, height: 300, width: 400, crop: :fill if item.photo.present? %>);">
      <%= link_to item_path(item), class: "rent-link" do%>
        <p> See more </p>
        <h5>£ <%= item.price %></h5>
      <%end%>
  </div>
  </div>
  <% end %>
  </div>
</div>
<p><%= link_to 'Create new item', new_item_path %></p>
